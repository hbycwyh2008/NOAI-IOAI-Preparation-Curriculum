from typing import Sequence, TypeVar

T = TypeVar("T")


def linear_search(values: Sequence[T], target: T) -> int:
    """Return the target index, or -1 when absent."""
    # TODO: implement without using list.index
    raise NotImplementedError
