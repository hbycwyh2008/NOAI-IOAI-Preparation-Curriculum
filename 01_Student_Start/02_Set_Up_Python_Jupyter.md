# Set Up Python and Jupyter

Minimum environment:

```bash
python -m venv .venv
# Activate the environment, then:
pip install jupyter numpy pandas matplotlib scikit-learn torch torchvision torchaudio
```

Verify imports in a notebook. Record the Python and package versions in your repository.
