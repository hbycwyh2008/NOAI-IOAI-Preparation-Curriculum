# Hugging Face API Guide

Priority concepts:

`pipeline`, tokenizer, model loading, datasets, padding/truncation, sequence classification, audio classification, inference, and saving/loading checkpoints.
