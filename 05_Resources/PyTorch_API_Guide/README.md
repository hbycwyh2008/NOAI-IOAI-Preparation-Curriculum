# PyTorch API Guide

Priority interfaces:

`Tensor`, `Dataset`, `DataLoader`, `nn.Module`, common layers, loss functions, `autograd`, `optim.Adam`, `optim.AdamW`, device movement, model save/load, and mixed precision.
